{
  "name": "stencil-ssr-starter",
  "version": "0.0.1",
  "private": true,
  "main": "server.js",
  "description": "Stencil starter with server-side rendering",
  "scripts": {
    "build": "stencil build",
    "dev": "sd concurrent \"stencil build --dev --watch\" \"node server.js\"",
    "prestart": "npm run build",
    "start": "node server.js"

  },
  "dependencies": {
    "@stencil/core": "^0.15.2",
    "@stencil/router": "^0.3.1",
    "express": "^4.16.4",
    "semver": "^5.6.0"
  },
  "devDependencies": {
    "@stencil/dev-server": "latest",
    "@stencil/sass": "0.1.1",
    "@stencil/utils": "latest",
    "semver": "^5.6.0"
  },
  "engines": {
    "node": ">=6.11.0"
  }
}